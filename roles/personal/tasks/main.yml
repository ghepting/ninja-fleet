- name: Personal | Install applications
  when: ansible_facts['os_family'] == 'Darwin'
  become: false
  community.general.homebrew_cask:
    name:
      - 1password
      - google-chrome
      - google-drive
    state: present
  tags: [personal, apps]
